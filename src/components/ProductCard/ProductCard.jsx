import React from 'react';
import styles from './ProductCard.module.scss';

const ProductCard = () => {
  // TODO: Display product image, title, price, and add click handler to navigate
  return (
    <div className={styles.card}>
      <h3>Product Card</h3>
    </div>
  );
};

export default ProductCard;
